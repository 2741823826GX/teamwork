<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gaoxu.springboot.mapper.UserMapper">
    <!-- 里面写Sql语句-->
    <select id="findAllUser" resultType="User">
        select * from user
    </select>

    <!--resultType 是返回类型-->
    <select id="findUserById" resultType="User">
        select * from user where userId = #{userId}
    </select>

    <select id="findUserByNamePassword" resultType="User">
        select * from user where name = #{name} and password = #{password}
    </select>

    <insert id="addUser" parameterType="User">
        insert into user(name, password, phone, identity)values (#{name}, #{password}, #{phone}, #{identity})
    </insert>

    <!-- paramterType 是参数类型-->
    <update id="updateUser" parameterType="User">
        update user set name = #{name}, password = #{password} where userId = #{userId}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from user where userId = #{userId}
    </delete>
</mapper>